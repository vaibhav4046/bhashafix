# Locale rules

Optional per-locale engineering overrides live here. Prefer `Intl`, Unicode
properties and script metadata; add overrides only when a product requirement
cannot be expressed through standards.
