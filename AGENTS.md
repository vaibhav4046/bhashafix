# BhashaFix repository guardrails

- Preserve the judge promise: five canonical failures must become zero while English stays green.
- Never replace deterministic browser predicates with model confidence or hard-coded success.
- Keep the repair boundary narrow: `demo/zariya.css`, `demo/translations.ta.json`, and `demo/zariya-runtime.json`.
- The hosted experience is a clearly labelled deterministic replay and must work without secrets.
- Do not add authentication, payments, databases, teams, arbitrary-repository support, or automatic merging.
- Preserve Hindi, Tamil, Urdu, RTL, keyboard, mobile, reduced-motion, and download behavior.
- Do not weaken tests, suppress TypeScript errors, hide console failures, or fabricate command receipts.
- Run `npm run verify` after meaningful changes.
